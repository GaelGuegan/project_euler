#include "problem_19.h"
#include "utils.h"
#include <string.h>

int main(int argc, char* argv[])
{
    if (parse_opts(argc, argv) != 0)
        exit(1);

    cout << "Result: " << endl;

    return 0;
}
